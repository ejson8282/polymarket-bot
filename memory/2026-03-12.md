# 2026-03-12

- Kevin shared two external strategy writeups (RTF attachments) about Polymarket fill-risk hardening and asked to evaluate adoptable ideas.
- Consensus from review:
  - Adopt: multi-signal parallel fill detection (user WS as primary, polling as fallback), event-level shutdown, dedupe/debounce, degraded-mode protection.
  - Caution: do not blindly use "WS disconnect => immediate cancelAll"; use threshold-based escalation. Avoid permanently running 1s polling due to rate-limit pressure; use dynamic intervals.
  - Escalation policy: event-first `cancelMarketOrders(condition)` + banlist/manual unban; reserve `cancelAll()` for system-level blindness/degraded state.
- Assistant confirmed these ideas align with Kevin’s hard constraint: prefer false positives over false negatives ("宁可误撤，不可漏报").
- Next implementation direction reaffirmed: integrate this framework into the existing `overnight_reprice.py` execution path.
