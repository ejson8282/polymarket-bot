# 2026-03-11

- Kevin要求实盘执行，核心目标：按奖励区间挂单、尽量不成交、并提升本金利用率（希望接近95%）。
- 期间确认了关键约束：
  - 同一时刻总挂单占用不能超过可用本金/allowance（出现多次 `not enough balance / allowance`）。
  - 价格必须按“订单簿 + 奖励区间”定价，不能机械用0.01。
- 对市场 `will-the-iranian-regime-fall-by-june-30` 进行了多轮操作：
  - 成功挂出过约150U梯度单（如0.20/0.19/0.18/0.17）。
  - 用户指出18/17可能不在当下奖励窗口，要求更智能实时调整。
  - 发现接口盘口有时返回极端值（bb=0.01/ba=0.99），与前端观测不一致，导致策略判断偏差。
- 执行层面修正：
  - 采用“先撤再挂”流程，`cancel_market_orders` 在该场景不稳定；改用按订单ID批量撤单 `cancel_orders(ids)` 可可靠清空 live 单。
  - 随后重挂并确认 live：0.20×200、0.19×200、0.18×200（YES token）。
- 用户最终关注点：
  - 不要“看起来在跑但价格错位”；
  - 需要夜间稳定运维（可持续挂单、按奖励窗口定价、避免无效撤单/重复报错）。
