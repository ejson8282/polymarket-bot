## Scope

- Owner: `domain:polymarket`
- Base branch and full SHA:
- Head branch and full SHA:
- Files and services:
- Runtime mode: `LIVE` / `PAPER` / `SHADOW` / `RESEARCH` / `REFERENCE`

## Coordination

- Shared-file lease issue, or `not required`:
- Overlapping PRs checked:
- Cross-repository dependencies and required order:
- Dashboard contract/display change required:

## Change and validation

- What changed and why:
- Tests and static checks:
- Known failures or checks not run:

## Release boundary

- [ ] This PR does not imply deployment, restart or real-money authorization.
- Target and previous/rollback full SHA:
- Post-deploy checks:
